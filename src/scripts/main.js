'use strict';

$('.shop__slider').slick({
  infinite: true,
  slidesToShow: 3,
  slidesToScroll: 3,
  // variableWidth: true

  nextArrow: '.shop__prev',
  prevArrow: '.shop__next',
});
/* eslint-env jquery */
